"""Единая точка подключения PyMuPDF.

Библиотека сменила имя: раньше она называлась ``fitz``, с версии 1.24 —
``pymupdf``. Старое имя пока работает, но при каждом обращении печатает
предупреждение об устаревании, а оно попадает в журнал приложения и мешает
читать настоящие сообщения.

Поэтому во всей программе следует писать::

    from .mupdf import fitz

Тогда на новых версиях берётся новое имя, на старых — прежнее, и ни одна из
них не жалуется.
"""

from __future__ import annotations

try:  # PyMuPDF 1.24 и новее
    import pymupdf as fitz
except ImportError:  # более старые выпуски
    import fitz  # noqa: F401

__all__ = ["fitz"]
