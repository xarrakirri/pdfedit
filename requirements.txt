# Обязательные зависимости pdfedit
pikepdf>=8.0        # низкоуровневая работа с объектами PDF (обёртка над qpdf)
PyMuPDF>=1.24       # отрисовка страниц, метрики стандартных шрифтов
fonttools>=4.40     # чтение и правка программ шрифтов, подмножества глифов

# Tkinter входит в стандартную библиотеку Python.
# В Linux его иногда надо доставить отдельно: apt install python3-tk
